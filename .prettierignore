node_modules
.next
out
build
coverage
test-results
playwright-report
blob-report
package-lock.json
data/corpus
# recorded demo goldens are machine-generated (compact JSON.stringify) and SACRED
# — their bytes hash into cache keys; never reformat them.
data/demo-cache
public/artifacts
public/pdf.worker.min.mjs

# hand-authored project docs — not code, not prettier's job (AGENTS.md rule 7: stay in your lane)
AGENTS.md
BUILD-BRIEF.md
terms.md
docs/
context/
CLAUDE.md
.claude/
# explicit re-ignore: the .gitignore negation (!.claude/skills/) un-ignores this
# for prettier v3's combined ignore stack; vendored skills stay byte-identical
# to ~/.claude/skills — never reformat.
.claude/skills/

# machine-generated pitch-deck build (user-owned, not app code)
presentation/

# frozen pre-refactor computeModel() snapshot — the byte-identical gate for
# lib/model (tests/model/compute.test.ts); machine-generated, never reformat.
tests/fixtures/model-base-snapshot.json
