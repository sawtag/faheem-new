# deps
node_modules/

# next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env — real keys never committed; .env.example IS committed
.env
.env.*
!.env.example

# tests
coverage/
test-results/
playwright-report/
blob-report/

# logs & OS noise
*.log
.DS_Store
Thumbs.db

# editors / local tooling
.vscode/
.idea/
.claude/

# runtime PDF uploads (live-mode only, never committed — see lib/uploads.ts)
data/uploads/

# NOTE — deliberately TRACKED (do not add here): data/corpus/ (verified PDFs),
# data/demo-cache/ (golden recordings = the offline demo), data/*.json,
# public/artifacts/ (pre-generated fallback deliverables), package-lock.json.
# A fresh clone must be demo-ready on the backup laptop.

# user-owned pitch deck workspace — never commit
presentation/

# demo-day local assets (upload-beat PDFs etc.) — never commit
demo-assets/

# uploaded company-template binary (user content — meta ships as null)
data/company-template/
